import { CARD_ASPECT_RATIO } from "./geometry.constants";

export const TEXTURE_WIDTH = 3072;
export const TEXTURE_HEIGHT = Math.round(TEXTURE_WIDTH / CARD_ASPECT_RATIO);
export const PADDING_X = 152;
export const PADDING_Y = 112;
